<div class="flex flex-col h-full">
  <div class="text-3xl font-bold text-slate-900 dark:text-slate-100">
    <%= data[:value] || 0 %>
  </div>
  <div class="text-sm text-slate-600 dark:text-slate-400 mt-1">
    <%= data[:label] || "Metric" %>
  </div>
  <% if data[:change] %>
    <div class="flex items-center mt-2">
      <% if data[:trend] == "up" %>
        <svg class="h-4 w-4 text-teal-500 dark:text-teal-400 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
        </svg>
        <span class="text-sm text-teal-600 dark:text-teal-400">+<%= data[:change] %>%</span>
      <% else %>
        <svg class="h-4 w-4 text-red-500 dark:text-red-400 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 17h8m0 0V9m0 8l-8-8-4 4-6-6"></path>
        </svg>
        <span class="text-sm text-red-600 dark:text-red-400"><%= data[:change] %>%</span>
      <% end %>
    </div>
  <% end %>
</div>