<div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 flex items-center justify-center">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <h1 class="text-5xl font-bold text-gray-900 mb-4">
      🚀 QuantumQuery
    </h1>
    <p class="text-xl text-gray-600 mb-8">
      Natural Language Data Science Platform
    </p>
    <div class="bg-white rounded-lg shadow-lg p-8">
      <p class="text-gray-700 mb-6">
        Ask questions in plain English. Get advanced analytics powered by AI.
      </p>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-left">
        <div class="p-4 bg-blue-50 rounded">
          <h3 class="font-semibold text-blue-900">🤖 Multi-Model AI</h3>
          <p class="text-sm text-blue-700">Claude, GPT-4, Gemini, and more</p>
        </div>
        <div class="p-4 bg-green-50 rounded">
          <h3 class="font-semibold text-green-900">📊 Code Generation</h3>
          <p class="text-sm text-green-700">Python, R, SQL automatically</p>
        </div>
        <div class="p-4 bg-purple-50 rounded">
          <h3 class="font-semibold text-purple-900">🔒 Secure Execution</h3>
          <p class="text-sm text-purple-700">Sandboxed Docker containers</p>
        </div>
      </div>
      <% if user_signed_in? %>
        <div class="mt-8">
          <p class="text-gray-600">Welcome back, <%= current_user.email %>!</p>
          <%= link_to "Sign Out", destroy_user_session_path, method: :delete, 
              class: "mt-4 inline-block bg-red-600 text-white px-6 py-2 rounded hover:bg-red-700" %>
        </div>
      <% else %>
        <div class="mt-8 space-x-4">
          <%= link_to "Sign In", new_user_session_path, 
              class: "inline-block bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700" %>
          <%= link_to "Sign Up", new_user_registration_path, 
              class: "inline-block bg-green-600 text-white px-6 py-2 rounded hover:bg-green-700" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
